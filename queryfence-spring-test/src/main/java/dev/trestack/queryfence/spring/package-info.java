/**
 * Spring TestContext support. Adding the dependency and a {@code queryfence.yml} to the test
 * classpath is the whole setup; the only type here is {@link
 * dev.trestack.queryfence.spring.QueryFencePolicy}, which names another policy file for a test
 * class.
 */
package dev.trestack.queryfence.spring;
