version: 1
mode: REPORT

rules:
  - id: tenant-isolation
    type: require-predicate
    column: tenant_id
    tables: [purchase_order]

suppressions:
  - rule: tenant-isolation
    origin: com.acme.orders.OrderRepository#listEverything
    reason: An admin screen that no longer exists under that name.
