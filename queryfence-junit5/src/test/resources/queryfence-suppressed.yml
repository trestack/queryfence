version: 1

rules:
  - id: tenant-isolation
    type: require-predicate
    column: tenant_id
    tables: [purchase_order]

suppressions:
  - rule: tenant-isolation
    origin: com.acme.orders.OrderRepository#findByStatus
    reason: Admin screen that lists the orders of every tenant.
