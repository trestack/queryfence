# Documentation site toolchain, pinned so a new MkDocs release cannot break the site on its own.
# To upgrade: bump a version here, run `mkdocs build --strict` locally, and commit both.
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
pymdown-extensions==12.1
Pygments==2.21.0
